// Criando um array de carros
let carros = ["BMW", "Fiat", "VW", "Renault", "Audi"];

// Revertendo a ordem dos elementos no array e imprimindo o array revertido
console.log(carros.reverse());

// Criando um array de números
let nums = [1, 2, 3, 45, 6, 7, 7];

// Revertendo a ordem dos elementos no array e imprimindo o array revertido
console.log(nums.reverse());


// apenas reverte os valores dentro da array